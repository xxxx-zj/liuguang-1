{
  "name": "video-watermark-remover",
  "version": "1.0.0",
  "description": "一个简单的视频去水印工具",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r public dist/ && cp server.js dist/ && cp package.json dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "fluent-ffmpeg": "^2.1.2",
    "cors": "^2.8.5"
  }
}